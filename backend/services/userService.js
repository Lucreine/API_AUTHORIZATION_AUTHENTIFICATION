const User = require('../models/articlesModels')

const register = async () => {
    return await Article.find();
}

const login = async () => {
    return await Article.find();
}

module.exports = {
    register,
    login
};
